steps:
- name: jenkins
  image: projectnebula/jenkins-job-runner:latest
  spec:
    masterURL: http://dev-001.cspconsultoria.com.br:8080/
    credentials: 
      method: http
      user: luiz
      token: 
        $type: Secret
        name: token
    job: ParameterBuild
    parameters: 
      cmd: echo